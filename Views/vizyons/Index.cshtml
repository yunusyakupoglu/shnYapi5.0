@model IEnumerable<shnYapi5._0.Entity.vizyon>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutEdit.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.arz)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.talep)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.content)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.arz)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.talep)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.content)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.vizyonID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.vizyonID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.vizyonID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
